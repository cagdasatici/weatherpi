# Copy to server/.env and edit values. Keep this file out of version control.
OPENWEATHER_API_KEY=replace_with_real_openweather_key
# Optional: set a proxy token that clients must send via X-Proxy-Token header
API_PROXY_TOKEN=replace_with_strong_random_token
